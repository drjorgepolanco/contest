<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= full_title(yield(:title)) %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    
    <div class="row">
      <%= render 'layouts/header' %>
    </div>

    <div class="row">
      <div class="large-12 medium-12 small-12 columns">
        <% flash.each do |key, value| %>          
          <%= content_tag(:div, value, class: "alert alert-#{key}") %>
        <% end %>
      </div>
    </div>

    <div class="row">
      <div id="body" class="large-12 medium-12 small-12 columns container">

        <div id="dashboard" class="large-2 medium-2 columns container left show-for-large-up">
          <%= render 'shared/dashboard' %>
        </div>

        <div id="feed" class="large-2 medium-2 columns container right show-for-large-up">
          <%= render 'shared/feed' %>
        </div>

        <div id="main" class="large-8 medium-12 small-12 columns large-centered container">
          <%= yield %>
        </div>
        
      </div>
    </div>

    <div class="row">
      <div class="large-12 medium-12 small-12 columns">
        <%= render 'layouts/footer' %>
      </div>
    </div>

    <%= javascript_include_tag "application" %>
    
  </body>

</html>

